# [库名] — 架构设计

## 1. 库画像（静态分析）

| 项 | 结论 |
|----|------|
| **C 库名称与角色** | |
| **主要头文件** | |
| **API 形态** | opaque / ops 表 / 值结构体 / 回调 |
| **inline / 宏 / union** | 有 / 无；说明 |
| **资源模型** | create/destroy / 引用计数 / 其他 |
| **线程安全** | |
| **分发与依赖** | 系统包 / vendored / 其他原生库 |
| **许可证** | |

## 2. 设计目标与约束

- **发布范围**：（crates.io / 内部）
- **MSRV**：（若已知）
- **非目标**：（如不覆盖 C++、不做性能调优）

## 3. Crate 与工作区划分

```text
[workspace 树或 crate 列表]
```

| Crate | 职责 | 对外发布 |
|-------|------|----------|
| `*-sys` | FFI + `build.rs` | 是/否 |
| 主 safe crate | | |
| （可选）macros / errors / async / 协议适配 | | |

## 4. 架构决策摘要

| 决策点 | 选择 | 理由（简短） |
|--------|------|--------------|
| 是否独立 `-sys` | | |
| safe 层厚度 | | |
| 绑定主路径 | | |
| `links` 键 | 有/无 | |
| 错误映射层级 | `-sys` 保持 C / safe 统一 `Result` | |

## 5. 依赖与许可证

- **传递原生依赖**：（如 zlib、ssl）
- **Rust 依赖原则**：（如 `foreign-types`、`thiserror`）
- **下游许可注意**：

## 6. 风险与未决项

| 项 | 说明 | 计划 |
|----|------|------|
| | | |
