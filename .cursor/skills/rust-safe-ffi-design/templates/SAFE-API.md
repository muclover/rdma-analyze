# [库名] — Safe API 设计

## 1. 设计原则（本库）

- `unsafe` 收口位置：
- 公开 API 稳定性承诺：
- 是否 re-export `-sys`：

## 2. 模块结构（草案）

```text
src/
├── lib.rs
├── error.rs
├── [core].rs
└── ...
```

| 模块 | 职责 |
|------|------|
| | |

## 3. 核心类型

| Rust 类型 | 对应 C | 所有权 / `Drop` | `Send`/`Sync` |
|-----------|--------|-----------------|---------------|
| | | | |

## 4. 错误模型

| 层 | 类型 | 映射规则 |
|----|------|----------|
| `-sys` | | 保持 C 语义 |
| safe | `Result<_, Error>` | |

## 5. `unsafe` 边界与 `# Safety`

### 5.1 集中 `unsafe` 的函数/模块

- 

### 5.2 C 契约表（草案）

| C API / 场景 | 前置条件 | 后置条件 | 不得违反 |
|--------------|----------|----------|----------|
| | | | |

## 6. 回调（若有）

- 线程要求：
- panic 策略：
- `user_data` 生命周期：

## 7. 暂不封装范围

| C API / 区域 | 原因 | 后续 |
|--------------|------|------|
| | | |

## 8. 示例入口（计划）

| 示例 | 演示 |
|------|------|
| | |
