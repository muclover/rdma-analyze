# [库名] — Async 与上层协议分析

## 1. 结论

| 项 | 决定 |
|----|------|
| **是否需要 crate 内 async API** | **需要 / 不需要** |
| **是否需要独立 async 适配 crate** | 需要 / 不需要 |
| **是否需要上层协议 crate**（gRPC/QUIC/自定义传输） | 需要 / 不需要 |

## 2. 分析依据

### 2.1 C 库并发模型

- 阻塞调用 / fd / 多路复用 / 回调 / 无：
- 线程安全说明：

### 2.2 产品需求（Q10）

- 目标用户是否要求 `.await`：
- 是否已有 Tokio/async-std 生态约束：

## 3. 若不需要 async

- **理由**：（同步 API 足够 / 由上层线程池封装 / …）
- **推荐替代**：（如 README 指向 `async-*` crate、仅暴露 `multi` 式同步接口）
- **N/A 影响**：（调用方如何处理阻塞、等待或回调）

## 4. 若需要 async

### 4.1 集成层级

| 方案 | 说明 | 是否采用 |
|------|------|----------|
| safe 层直接 `async fn` | | |
| 独立 `foo-async` crate | | |
| 仅 example 演示 | | |

### 4.2 与 C 等待机制

- fd + `AsyncFd` / 自建 reactor / 线程池阻塞：
- CQ/完成事件类 API（若适用）：

### 4.3 `Send` / `Sync`

- 句柄是否跨任务：
- 限制（如 `!Send` until pinned）：

## 5. 上层协议 crate（Q9）

| 项 | 内容 |
|----|------|
| 是否需要 | |
| 边界 | FFI 止于 [层]；协议类型不新增 `extern "C"` |
| 计划 crate 名与职责 | |

## 6. 未决项

| 项 | 计划 |
|----|------|
| | |
